name: climate_similarity_measures
channels:
  - defaults
dependencies:
  - basemap
  - seaborn
  - jupyter
  - python=3.6
  - joblib
  - pip
  - pip:
    - pyinform
    - sklearn
    - similaritymeasures
